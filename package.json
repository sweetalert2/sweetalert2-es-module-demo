{
  "name": "sweetalert2-es-module-demo",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "@sweetalert2/themes": "^3.1.0",
    "sweetalert2": "^9.5.3"
  },
  "devDependencies": {
    "http-server": "^0.12.0",
    "jest": "^25.0.0",
    "jest-image-snapshot": "^2.11.1",
    "puppeteer": "^2.0.0"
  },
  "scripts": {
    "build": "cp --recursive node_modules/sweetalert2 node_modules/@sweetalert2 .",
    "serve": "http-server -p 5500",
    "test": "jest --env=node",
    "update_screenshots": "jest -u --env=node"
  }
}
